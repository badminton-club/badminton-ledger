# ui

The React app for Badminton Ledger. See the [repo root README](../README.md) for what this app
does, setup instructions, and deployment.

This project was bootstrapped with [Create React App](https://github.com/facebook/create-react-app);
`npm start`, `npm test`, and `npm run build` all work as usual.
